# Copy this file to `.env.local` (create it manually) and fill values.
#
# MongoDB Atlas connection string (replace <...>)
# Example:
# MONGODB_URI="mongodb+srv://<username>:<password>@<cluster>.mongodb.net/adarsh_medical?retryWrites=true&w=majority&appName=<appName>"
MONGODB_URI=""

# Used to sign the admin session cookie (set a long random string)
JWT_SECRET=""

# One-time setup key required to create the first admin at /setup
SETUP_KEY=""




